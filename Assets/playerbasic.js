#pragma strict

var sample : GameObject;

function Start () {

}

function Update () {

      /*  var RMB : boolean = Input.GetMouseButtonDown(1);

        if (RMB)
        {

            var hit : RaycastHit;
            var ray : Ray = Camera.main.ScreenPointToRay(Input.mousePosition);

            if (Physics.Raycast(ray, out hit) && hit.transform.tag == "BluePlayerTag")
            {
                  Instantiate(sample,hit.transform.position,hit.transform.rotation);
              //  RecievedMove(hit.point);
            }


        }
         */
}